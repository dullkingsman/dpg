---
title: "dpg completion bash"
generated: true
---

## dpg completion bash

Generate the autocompletion script for bash

### Synopsis

Generate the autocompletion script for the bash shell.

This script depends on the 'bash-completion' package.
If it is not installed already, you can install it via your OS's package manager.

To load completions in your current shell session:

	source <(dpg completion bash)

To load completions for every new session, execute once:

#### Linux:

	dpg completion bash > /etc/bash_completion.d/dpg

#### macOS:

	dpg completion bash > $(brew --prefix)/etc/bash_completion.d/dpg

You will need to start a new shell for this setup to take effect.


```
dpg completion bash
```

### Options

```
  -h, --help              help for bash
      --no-descriptions   disable completion descriptions
```

### Options inherited from parent commands

```
  -C, --dir string   project root directory (default: current working directory)
      --env string   path to .env file (default: .env in project root, if present)
```

### SEE ALSO

* [dpg completion](/docs/cli/dpg_completion/)	 - Generate the autocompletion script for the specified shell

###### Auto generated by spf13/cobra on 8-May-2026
