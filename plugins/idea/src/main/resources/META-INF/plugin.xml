<idea-plugin>
  <id>dev.thec1oud.plugins.ide.intellij.dpg</id>
  <name>DPG</name>
  <version>0.2.0</version>
  <vendor url="https://github.com/thec1oud/dpg">thec1oud</vendor>

  <description><![CDATA[
    <p>Native language support for <b>.dpg</b> (DPG / Declarative PostgreSQL) source files.</p>
    <ul>
      <li>Syntax highlighting — DPG object keywords, block directives, SQL bodies, dollar-quoted strings</li>
      <li>Brace and parenthesis matching</li>
      <li>Code folding for <code>{ }</code> blocks and dollar-quoted function bodies</li>
      <li>Structure view — schema/object hierarchy in the Project sidebar</li>
      <li>Code completion — DPG object types and block directives</li>
      <li>Error annotations — DPG-E006 forbidden verbs, DPG-E007 MACRO inside block</li>
      <li>Find Usages for named objects</li>
      <li>Code formatting — blank lines between objects, indented <code>{ }</code> block content (Ctrl+Alt+L)</li>
      <li>Line and block comment actions (<code>-- …</code> and <code>/* … */</code>)</li>
      <li>Customisable colours in Settings → Editor → Color Scheme → DPG</li>
    </ul>
    <p>All features work offline with no external binary required.</p>
  ]]></description>

  <depends>com.intellij.modules.platform</depends>

  <extensions defaultExtensionNs="com.intellij">

    <!-- ── File type ──────────────────────────────────────────────────────── -->
    <fileType
        name="DPG"
        implementationClass="dev.thec1oud.plugins.ide.intellij.dpg.DpgFileType"
        language="DPG"
        extensions="dpg"
        fieldName="INSTANCE"/>

    <!-- ── Language foundation ───────────────────────────────────────────── -->
    <lang.parserDefinition
        language="DPG"
        implementationClass="dev.thec1oud.plugins.ide.intellij.dpg.lang.DpgParserDefinition"/>

    <!-- ── Syntax highlighting ───────────────────────────────────────────── -->
    <lang.syntaxHighlighterFactory
        language="DPG"
        implementationClass="dev.thec1oud.plugins.ide.intellij.dpg.highlight.DpgSyntaxHighlighterFactory"/>

    <colorSettingsPage
        implementation="dev.thec1oud.plugins.ide.intellij.dpg.highlight.DpgColorSettingsPage"/>

    <!-- ── Editor services ───────────────────────────────────────────────── -->
    <lang.commenter
        language="DPG"
        implementationClass="dev.thec1oud.plugins.ide.intellij.dpg.editor.DpgCommenter"/>

    <lang.braceMatcher
        language="DPG"
        implementationClass="dev.thec1oud.plugins.ide.intellij.dpg.editor.DpgBraceMatcher"/>

    <lang.foldingBuilder
        language="DPG"
        implementationClass="dev.thec1oud.plugins.ide.intellij.dpg.editor.DpgFoldingBuilder"/>

    <!-- ── Structure view ────────────────────────────────────────────────── -->
    <lang.psiStructureViewFactory
        language="DPG"
        implementationClass="dev.thec1oud.plugins.ide.intellij.dpg.structure.DpgStructureViewFactory"/>

    <!-- ── Code completion ───────────────────────────────────────────────── -->
    <completion.contributor
        language="DPG"
        implementationClass="dev.thec1oud.plugins.ide.intellij.dpg.completion.DpgCompletionContributor"/>

    <!-- ── Semantic annotations ──────────────────────────────────────────── -->
    <annotator
        language="DPG"
        implementationClass="dev.thec1oud.plugins.ide.intellij.dpg.annotator.DpgAnnotator"/>

    <!-- ── Find Usages ───────────────────────────────────────────────────── -->
    <lang.findUsagesProvider
        language="DPG"
        implementationClass="dev.thec1oud.plugins.ide.intellij.dpg.findusages.DpgFindUsagesProvider"/>

    <!-- ── References (go-to-definition) ────────────────────────────────── -->
    <psi.referenceContributor
        language="DPG"
        implementation="dev.thec1oud.plugins.ide.intellij.dpg.reference.DpgReferenceContributor"/>

    <!-- ── Formatting ───────────────────────────────────────────────────── -->
    <lang.formatter
        language="DPG"
        implementationClass="dev.thec1oud.plugins.ide.intellij.dpg.formatter.DpgFormattingModelBuilder"/>

    <externalFormatProcessor
        implementation="dev.thec1oud.plugins.ide.intellij.dpg.formatter.DpgExternalFormatter"/>

  </extensions>
</idea-plugin>
