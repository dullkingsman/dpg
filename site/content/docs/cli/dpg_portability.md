---
title: "dpg portability"
generated: true
---

## dpg portability

Report PostgreSQL-specific constructs in use

### Synopsis

Parses the .dpg source files and reports all constructs that are
PostgreSQL-specific (not part of the ISO/IEC 9075 SQL standard).

Each reported construct includes the source location and the standard SQL
alternative where one exists. This command never blocks compilation.

```
dpg portability [flags]
```

### Options

```
      --cluster string    cluster to analyze (required when multiple clusters exist)
      --database string   database to analyze (required when multiple databases exist)
      --format string     output format: text or json (default "text")
  -h, --help              help for portability
```

### Options inherited from parent commands

```
  -C, --dir string   project root directory (default: current working directory)
      --env string   path to .env file (default: .env in project root, if present)
```

### SEE ALSO

* [dpg](/docs/cli/dpg/)	 - Declarative PG — schema compiler and migration tool

###### Auto generated by spf13/cobra on 16-May-2026
