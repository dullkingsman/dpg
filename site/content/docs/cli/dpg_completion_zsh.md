---
title: "dpg completion zsh"
generated: true
---

## dpg completion zsh

Generate the autocompletion script for zsh

### Synopsis

Generate the autocompletion script for the zsh shell.

If shell completion is not already enabled in your environment you will need
to enable it.  You can execute the following once:

	echo "autoload -U compinit; compinit" >> ~/.zshrc

To load completions in your current shell session:

	source <(dpg completion zsh)

To load completions for every new session, execute once:

#### Linux:

	dpg completion zsh > "${fpath[1]}/_dpg"

#### macOS:

	dpg completion zsh > $(brew --prefix)/share/zsh/site-functions/_dpg

You will need to start a new shell for this setup to take effect.


```
dpg completion zsh [flags]
```

### Options

```
  -h, --help              help for zsh
      --no-descriptions   disable completion descriptions
```

### Options inherited from parent commands

```
  -C, --dir string   project root directory (default: current working directory)
      --env string   path to .env file (default: .env in project root, if present)
```

### SEE ALSO

* [dpg completion](/docs/cli/dpg_completion/)	 - Generate the autocompletion script for the specified shell

###### Auto generated by spf13/cobra on 17-May-2026
